#include <stdio.h>

int main() {
    int number;
    printf("enter the number:");
    scanf("%d",&number);
    if(number > 0)
    printf(" this number is possitive %d",number);
    else
    printf("this number is negative%d",number);
}
