// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int r;
    float pi=3.14,area;
    printf("enter the radius of a circle:");
    scanf("%d",&r);
    area=pi*r*r;
    printf("area of a ciecle=%f",area);
}
