#include <stdio.h>

int main() {
    int number;
    printf("enter the value of number:");
    scanf("%d",&number);
    if(number % 2 == 0)
    printf("this number is even %d",number);
    else
    printf("this number is odd %d",number);
}
